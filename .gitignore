# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/

# Stray build output from older script versions
zambia_v2.pptx

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Temp files
*.tmp
*~

# Note: the deck (zambia_retail_v2.pptx) and the package-lock.json are
# committed on purpose — the deck is the deliverable, the lockfile keeps
# builds reproducible.
